# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
#
# ⚠️ DISABLED: Sostituito dal nuovo sistema di CI/CD ottimizzato
# Nuovo sistema: ci.yml gestisce preview deploy su richiesta
# Deploy produzione: deploy-on-release.yml al merge

name: '[DISABLED] Deploy to Firebase Hosting on PR'
on:
  # Disabilitato - trigger commentato
  # pull_request:
  #   paths:
  #     - 'client/**'
  #     - 'server/**'
  #     - 'shared/**'
  #     - 'public/**'
  #     - 'package.json'
  #     - 'package-lock.json'
  #     - 'vite.config.ts'
  #     - 'tsconfig.json'
  - 'tailwind.config.ts'
  - 'postcss.config.js'
  - 'firebase.json'
  - '.github/workflows/firebase-hosting-pull-request.yml'
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Install dependencies
        run: npm ci
      - name: Run type checking
        run: npm run check
      - name: Run smart test strategy (PR optimization)
        run: CI=true npm run test:smart
      - name: Run tests with coverage
        run: npm run test:coverage

  build_and_preview:
    needs: test
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Install dependencies and build
        run: npm ci && npm run build:prod
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_LECCE_DIGITAL_LEGENDS }}
          projectId: lecce-digital-legends
